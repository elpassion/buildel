'use client';

// import { Loader } from '~/components';
// import { useOrganizationsQuery } from '~/modules/Api/Organizations';

export const OrganizationsClient = () => {
  // TODO (hub33k): implement organizations
  // const { data, isLoading, isError, ...rest } = useOrganizationsQuery();
  // if (isLoading || !data) {
  //   return <Loader />;
  // }
  // if (isError) {
  //   return <p>Oops, something went wrong!</p>;
  // }

  return (
    <>
      <p>List organizations here</p>
    </>
  );
};
